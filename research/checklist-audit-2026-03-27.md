# 格物（Gewu）清单审计（2026-03-27）

审计范围：`apps/web`、`apps/api`、`apps/admin`、`scripts/smoke-*.mjs`  
结论标签：`已完成` / `部分完成` / `未完成`

## 一、用户账号

- 注册 / 登录 / 登出流程：`已完成`
- 登录方式（先确认再做）：`已完成`（当前仅邮箱密码，Google OAuth 已按你的要求暂不做）
- 邮箱验证：`已完成`（未验证邮箱禁止登录）
- 找回密码：`已完成`（忘记密码 + 重置密码）
- Token 过期跳登录：`已完成`（后端 401 + 前端会话校验）
- 未登录访问付费功能跳转：`部分完成`（关键提交动作会跳登录；部分功能页对标 speedai 做了未登录回到 `/zh/AI-search`）

## 二、积分 / 按次计费

- 服务端扣减：`已完成`
- 调用前检查积分：`已完成`
- AI 调用失败退款：`已完成`
- 并发重复扣减防护：`已完成`（用户级锁）
- 积分变动日志：`已完成`（points records）
- 用户查看积分和明细：`已完成`
- 积分不足提示和引导：`已完成`

## 三、支付 / 充值

- 回调服务端验签：`已完成`（mock 链路）
- 重复回调幂等：`已完成`
- 成功到账反馈：`已完成`
- 失败友好提示：`已完成`
- 充值记录查询：`已完成`
- 退款场景：`已完成`（按可退积分处理，支持幂等）
- 沙盒密钥切正式密钥：`未完成`（真实网关未接入，当前 mock）

## 四、AI 文件处理

- 文件类型白名单：`已完成`（docx/pdf/txt）
- 单文件大小限制：`已完成`
- 文件存储位置与保留策略：`未完成`（真实 OSS 生命周期策略待接）
- 用户间文件隔离：`部分完成`（下载票据鉴权完成；真实 OSS 鉴权待接）
- 长任务进度提示：`已完成`（docx 状态接口返回进度）
- 失败提示与退款：`已完成`
- 流式输出与非流式一致性：`已完成`（长文本 SSE，普通任务轮询）
- 超时处理：`已完成`

## 五、AI 代码 / 写作辅助

- 输入长度限制：`已完成`
- 无效输入过滤：`已完成`
- 流式输出：`已完成`（长文本任务）
- 中途取消与积分处理：`已完成`
- 频率限制：`已完成`
- 历史记录：`部分完成`（后端有任务列表接口，前端独立“历史页”待补强）

## 六、权限与安全

- AI 接口登录验证：`已完成`
- 积分检查与扣减在服务端：`已完成`
- API Key 不硬编码：`已完成`
- 修改参数跳过扣费：`已完成`（服务端强校验）
- 管理端独立登录与隔离：`已完成`

## 七、管理后台

- 用户列表/注册时间/积分余额：`已完成`
- 手动加减积分 + 记录：`部分完成`（积分变动有记录；“管理员操作日志”独立表待补）
- 充值订单与状态：`已完成`
- AI 调用量与费用趋势：`已完成`
- 封禁用户：`已完成`

## 八、上线前清理

- console.log/调试输出：`部分完成`（主链路已收敛，待全仓最终扫尾）
- 测试账号/测试积分/假数据：`未完成`（当前仍有 seed 数据）
- 沙盒支付密钥切正式：`未完成`
- 关闭开发模式与错误脱敏：`部分完成`（后端错误处理已脱敏，部署侧待最终开关）

## 九、基础能力

- 404 页面：`已完成`
- 友好错误页：`已完成`
- 错误监控：`已完成`（已接客户端上报 + 服务端监控钩子）
- 用户协议/隐私政策：`已完成`
- 注册确认邮件/支付成功邮件：`已完成`

## 当前优先级（下一轮按这个顺序推进）

1. 真实 OSS 上传与生命周期策略（补齐文件链路短板）
2. 管理员操作日志（积分调整、封禁、模型配置改动）
3. 视觉对齐继续压差（工作台壳层 + 首页卡片 + 输入区）
4. 上线前清理项自动化（seed/调试输出/部署开关）
